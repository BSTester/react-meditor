export type Props = {
    meditorPath: string,
    style: object,
    imageUpload: string,
    initValue: object,
    headers: object,
    readonly: boolean,
    onChange: (value: object) => void,
}
  