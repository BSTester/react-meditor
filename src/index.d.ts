export type Props = {
    meditorPath: string,
    style: object,
    imageUpload: string,
    initValue: object,
    headers: object,
    readonly: boolean,
    onChange: (e: object) => void,
    onLoad: (e: object) => void,
}
  